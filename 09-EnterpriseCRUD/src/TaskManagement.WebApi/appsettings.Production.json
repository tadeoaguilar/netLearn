{
  "Database": {
    "Provider": "postgres"
  },
  "Jwt": {
    "Audience": "taskmanagement-api"
  },
  "_comment": [
    "Jwt:Authority is deliberately NOT set here.",
    "Set it per-deployment, over HTTPS, e.g.:",
    "  Jwt__Authority=https://keycloak.example.com/realms/taskmanagement",
    "An http:// authority is refused outside Development, because signing keys",
    "fetched over plaintext can be substituted by anyone on the network path."
  ]
}
